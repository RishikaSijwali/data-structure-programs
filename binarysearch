#include<stdio.h>
int main()
{
	int n=5;
	int a[5]={1,2,6,8,5,4};
	int start=0,end,mid,i data;
	scanf("%d",&data);
	end=n-1;
	mid=(start +end)/2;
	while(data!=a[mid]&&start<=end)
	{
		if(data>a[mid])
		start=mid+1;
		else if
		{
		end=mid-1;
		mid=(start +end)/2;	
		}
		else if(data==a[mid])
		printf("%d,mid++");
		else if(start>end)
		printf("not");
	}
}
